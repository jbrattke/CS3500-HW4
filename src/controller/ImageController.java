package controller;

/**
 * Interface for the controller that will run the program, taking in commands from the user
 * and allocating tasks accordingly to the view and model.
 */
public interface ImageController {

  /**
   * This method runs the program.
   */
  void run(); //throws IllegalStateException, IllegalArgumentException
}
